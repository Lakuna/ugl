/**
 * An object representing a debug session of a WebGL rendering context.
 * @public
 */
export default interface DebugInfo {
	/** Whether or not debug information should be collected and logged. */
	isActive: boolean;
}
