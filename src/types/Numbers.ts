/** A single row of 2 numbers. */
export type Numbers1x2 = [number, number];

/** A single row of 3 numbers. */
export type Numbers1x3 = [number, number, number];

/** A single row of 4 numbers. */
export type Numbers1x4 = [number, number, number, number];

/** Numbers arranged in two rows and two columns. */
export type Numbers2x2 = [
	number, number,
	number, number
];

/** Numbers arranged in three rows and three columns. */
export type Numbers3x3 = [
	number, number, number,
	number, number, number,
	number, number, number
];

/** Numbers arranged in four rows and four columns. */
export type Numbers4x4 = [
	number, number, number, number,
	number, number, number, number,
	number, number, number, number,
	number, number, number, number
];
