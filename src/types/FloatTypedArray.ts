/**
 * Array-like objects that contain floating-point numbers and provide a
 * mechanism for reading and writing raw binary data in memory buffers.
 * `Float64Array` is excluded because it cannot be used with WebGL.
 */
export type FloatTypedArray = Float32Array;
