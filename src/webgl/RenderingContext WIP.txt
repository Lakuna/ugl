TODO
	All standard interfaces except WebGLRenderingContext
	WebGLRenderingContext.getParameter()
	WebGLRenderingContext "Uniforms and attributes," "Drawing buffers," and "Working with extensions" categories

renderingContext				canvas.getContext()
	canvas					gl.canvas
	drawingBuffer
		width				gl.drawingBufferWidth
		height				gl.drawingBufferHeight
	contextAttributes			gl.getContextAttributes()
		hasAlpha
		hasDepth
		hasStencil
		isDesynchronized
		usesAntiAliasing
		requiresPerformantHardware
		assumesPremultipliedAlpha
		preservesDrawingBuffer
		isXRCompatible			gl.makeXRCompatible()
	isLost					gl.isContextLost()
	scissorBox				gl.scissor()
		x
		y
		width
		height
		enabled				gl.enabled()
	viewport				gl.viewport()
		x
		y
		width
		height
	textureUnit
		active				gl.activeTexture()
		max				gl.getParameter()
	blend
		color				gl.blendColor()
			red
			green
			blue
			alpha
		equation			gl.blendEquationSeparate()
			rgb
			alpha
		function			gl.blendFuncSeparate()
			sourceRGB
			destinationRGB
			sourceAlpha
			destinationAlpha
		enabled				gl.enable()
	color
		default				gl.clearColor()
			red
			green
			blue
			alpha
		mask				gl.colorMask()
			red
			green
			blue
			alpha
	depth
		default				gl.clearDepth()
		function			gl.depthFunc()
		mask				gl.depthMask()
		range				gl.depthRange()
			near
			far
		polygonOffset			gl.polygonOffset()
			factor
			units
			enabled			gl.enable()
		enabled				gl.enable()
	stencil
		default				gl.clearStencil()
		function			gl.stencilFuncSeparate()
			front
				function
				reference
				mask
			back
				function
				reference
				mask
		mask				gl.stencilMaskSeparate()
			front
			back
		action				gl.stencilOpSeparate()
			front
				fail
				depthFail
				pass
			back
				fail
				depthFail
				pass
		enabled				gl.enable()
	cull
		face				gl.cullFace()
		frontFace			gl.frontFace()
		enabled				gl.enable()
	dither					gl.enable()
	sampleCoverage				gl.sampleCoverage()
		value
		invert
		sampleAlphaToCoverage		gl.enable()
		enabled				gl.enable()
	rasterizerDiscard			gl.enable()
	error					gl.getError()
	hint					gl.hint()
		generateMipmap
		fragmentShaderDerivative
	lineWidth				gl.lineWidth()
	pixelStorage				gl.pixelStorei()
		packAlignment
		unpackAlignment
		unpackFlipY
		unpackPremultiplyAlpha
		unpackColorspaceConversion
		packRowLength
		packSkipPixels
		packSkipRows
		unpackRowLength
		unpackImageHeight
		unpackSkipPixels
		unpackSkipRows
		unpackSkipImages
	boundBuffers				gl.bindBuffer()
		array
		elementArray
		copyRead
		copyWrite
		transformFeedback
		uniform
		pixelPack
		pixelUnpack

buffer						gl.createBuffer()
	static isValid()			gl.isBuffer()
	target					gl.bindBuffer()
	data					gl.bufferData()
	usage					gl.bufferData(), gl.getBufferParameter()
	insertData()				gl.bufferSubData()
	delete()				gl.deleteBuffer()
	size					gl.getBufferParameter()

framebuffer					gl.createFramebuffer()
	static isValid()			gl.isFramebuffer()
	target					gl.bindFramebuffer()
	status					gl.checkFramebufferStatus()
	data					gl.framebufferRenderbuffer(), gl.framebufferTexture2D()
	color[0-15], depth, stencil		gl.getFramebufferAttachmentParameter()
		objectType
		objectName
		textureLevel
		textureCubeMapFace
		colorEncoding
		alphaSize
		blueSize
		componentType
		depthSize
		greenSize
		redSize
		stencilSize
		textureLayer
		textureViews
		textureBaseViewIndex
	delete()				gl.deleteFramebuffer()
	toArray()				gl.readPixels()

renderbuffer					gl.createRenderbuffer()
	static isValid()			gl.isRenderbuffer()
	target					gl.bindRenderbuffer()
	data					gl.renderbufferStorage()
	internalFormat				gl.renderbufferStorage()
	width					gl.getRenderbufferParameter(), renderbufferStorage()
	height					gl.getRenderbufferParameter(), renderbufferStorage()
	internalFormat				gl.getRenderbufferParameter()
	greenSize				gl.getRenderbufferParameter()
	blueSize				gl.getRenderbufferParameter()
	redSize					gl.getRenderbufferParameter()
	alphaSize				gl.getRenderbufferParameter()
	depthSize				gl.getRenderbufferParameter()
	stencilSize				gl.getRenderbufferParameter()
	samples					gl.getRenderbufferParameter()
	delete()				gl.deleteRenderbuffer()

texture						gl.createTexture()
	static isValid()			gl.isTexture()
	target					gl.bindTexture()
	data					gl.compressedTexImage2D(), gl.compressedTexSubImage2D(), copyTexImage2D(), copyTexSubImage2D(), texImage2D(), texSubImage2D()
	level					^
	format					^
	internalFormat				^
	width					^
	height					^
	border					^
	imageSize				^
	offset					^
	sourceOffset				^
	sourceLengthOverride			^
	offsetX					^
	offsetY					^
	x					^
	y					^
	doGenerateMipmap			gl.generateMipmap()
	magnificationFilter			gl.getTexParameter(), gl.texParameterf()
	minificationFilter			gl.getTexParameter(), gl.texParameterf()
	wrappingFunctionS			gl.getTexParameter(), gl.texParameterf()
	wrappingFunctionT			gl.getTexParameter(), gl.texParameterf()
	maximumAnisotropy			gl.getTexParameter(), gl.texParameterf()
	baseLevel				gl.getTexParameter(), gl.texParameterf()
	comparisonFunction			gl.getTexParameter(), gl.texParameterf()
	comparisonMode				gl.getTexParameter(), gl.texParameterf()
	isImmutable				gl.getTexParameter()
	immutableLevels				gl.getTexParameter()
	maxLevel				gl.getTexParameter(), gl.texParameterf()
	maxLevelOfDetail			gl.getTexParameter(), gl.texParameterf()
	minLevelOfDetail			gl.getTexParameter(), gl.texParameterf()
	wrappingFunctionR			gl.getTexParameter(), gl.texParameterf()

program						gl.createProgram(), gl.attachShader(), gl.linkProgram()
	static isValid()			gl.isProgram()
	delete()				gl.deleteProgram()
	detachShader()				gl.detachShader()
	shaders					gl.getAttachedShaders()
	deleteStatus				gl.getProgramParameter()
	linkStatus				gl.getProgramParameter()
	validateStatus				gl.getProgramParameter()
	shaderCount				gl.getProgramParameter()
	attributeCount				gl.getProgramParameter()
	uniformCount				gl.getProgramParameter()
	transformFeedbackBufferMode		gl.getProgramParameter()
	transformFeedbackVaryingCount		gl.getProgramParameter()
	uniformBlockCount			gl.getProgramParameter()
	infoLog					gl.getProgramInfoLog()
	use()					gl.useProgram()
	isValid					gl.validateProgam()

shader						gl.createShader(), gl.compileShader(), gl.shaderSource()
	static isValid()			gl.isShader()
	delete()				gl.deleteShader()
	detachFrom()				gl.detachShader()
	deleteStatus				gl.getShaderParameter()
	compileStatus				gl.getShaderParameter()
	type					gl.getShaderParameter()
	infoLog					gl.getShaderInfoLog()
	source					gl.getShaderSource()

NOT USED
	gl.bindAttribLocation()